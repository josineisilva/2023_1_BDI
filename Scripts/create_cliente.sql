CREATE TABLE cliente (
                     codigo       CHAR(2)  PRIMARY KEY,
                     nome         CHAR(10) NOT NULL,
                     sobrenome    CHAR(10) NOT NULL
                     );
