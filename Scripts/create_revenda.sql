CREATE TABLE revenda (
                     codigo       CHAR(2)  PRIMARY KEY,
                     nome         CHAR(15) NOT NULL,
                     cidade       CHAR(10),
                     estado       CHAR(2)
                     );
